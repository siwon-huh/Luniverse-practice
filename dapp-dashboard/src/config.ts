namespace Config {
    export const ENVIRONMENT_ID = import.meta.env.LUNIVERSE_ENVIRONMENT_ID;
    export const KEY_ID = import.meta.env.LUNIVERSE_KEY_ID;
    export const KEY_SECRET = import.meta.env.LUNIVERSE_KEY_SECRET;
    export const AUTH_TOKEN = import.meta.env.LUNIVERSE_AUTH_TOKEN;
}

export default Config;
